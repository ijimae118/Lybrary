#include <stdio.h>
#include <conio.h>
int main(){
 int x,y;
 printf("Nhap so salary:");
 scanf("%d",&x);
 printf("\n========\nNhap so age:");
 scanf("%d",&y);
 printf("\n========\nSalary($) cua ban la= %d va age cua ban la= %d",x,y);
 return 0;
}

